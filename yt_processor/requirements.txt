# Legacy convenience mirror of the package's optional `[vector]` extra.
# The primary install path for this repo is now:
#   python -m pip install -e .[vector]
# The base CLI install (`python -m pip install -e .`) already pulls in yt-dlp.

openai
pinecone
tiktoken
tenacity
