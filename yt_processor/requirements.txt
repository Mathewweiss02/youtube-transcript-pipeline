# Core optional API / embedding dependencies for the published pipeline.
# The downloader / chunker flow relies on an external `yt-dlp` executable,
# not the Python package.

openai
pinecone
tiktoken
tenacity
